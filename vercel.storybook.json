{
  "buildCommand": "yarn storybook:build",
  "outputDirectory": "storybook-static",
  "framework": "vite",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD .storybook/ stories/",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
